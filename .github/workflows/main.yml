name: Main
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: node:18.11
      volumes:
        - ${PWD}:/cc-homepage
    steps:
      - name: Build homepage
        run: ls -la
